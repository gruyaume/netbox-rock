name: netbox
base: ubuntu@22.04
version: '3.6.5'
summary: Document your network.
description: |
      NetBox is the leading solution for modeling and documenting modern networks.
      By combining the traditional disciplines of IP address management (IPAM) and
      datacenter infrastructure management (DCIM) with powerful APIs and extensions,
      NetBox provides the ideal "source of truth" to power network automation. Read
      on to discover why thousands of organizations worldwide put NetBox at the
      heart of their infrastructure.

license: Apache-2.0
platforms:
    amd64:

parts:
    netbox:
        plugin: python
        source-type: git
        source-tag: v3.6.5
        source: https://github.com/netbox-community/netbox.git
